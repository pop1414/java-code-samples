# framework-basis/dynamic-proxy

定位：JDK 动态代理、CGLIB 与拦截器链

计划示例：
- JdkDynamicProxyDemo.java
- CglibProxyDemo.java

运行：
- 构建当前子模块：`mvn -pl framework-basis/dynamic-proxy -am test`
- 在本目录下直接执行：`mvn test`
