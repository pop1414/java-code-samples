# oop

定位：面向对象核心概念与实践

子模块概览：
- encapsulation：封装与访问控制，getter/setter 约定
- inheritance：继承、super、方法重写
- polymorphism：多态、向上/向下转型、动态绑定
- abstract：抽象类与接口的差异和适用场景
- inner-class：成员/静态/局部/匿名内部类
- enum：枚举定义、常用模式与策略式用法

运行：
- 构建当前父模块及子模块：`mvn -pl oop -am test`
- 在子模块内执行：`mvn test`
