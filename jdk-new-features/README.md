# jdk-new-features

定位：JDK 各版本的新特性示例

子模块概览：
- jdk8：Lambda、Stream、Optional、函数式接口
- jdk11：var 局部类型推断、新 HttpClient
- jdk17：密封类、switch 表达式增强

运行：
- 构建当前父模块及子模块：`mvn -pl jdk-new-features -am test`
- 在子模块内执行：`mvn test`
